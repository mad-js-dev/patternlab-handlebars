language: node_js
node_js:
- 12.9.0
cache: npm
deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    on: 
      branch: master

language: node_js
node_js:
  - 12.9.0
cache:
  directories:
  - node_modules
script:
  - npm run deploy
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master